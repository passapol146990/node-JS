<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-9ndCyUaIbzAi2FUVXJi0CjmCapSmO7SnpJef0486qhLnuZ2cdeRhO02iuK6FUUVM" crossorigin="anonymous">
    <!-- <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js" integrity="sha384-geWF76RCwLtnZ8qwWowPQNguL3RmwHVBC9FhGdlKrxdiJJigb/j/68SIy3Te4Bkz" crossorigin="anonymous"></script> -->
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>home</title>
</head>
<body>
    <h1>Hello</h1>
    <div class="border border-black container">
        <form method="POST" action="/savedata">
            <div>
                <label>name</label>
                <input name="name" class="form-control" type="text" placeholder="name...">
            </div>
            <div>
                <label>age</label>
                <input name="age" class="form-control" type="text" placeholder="age...">
            </div>
            <div>
                <label>salary</label>
                <input name="salary" class="form-control" type="number" placeholder="salary...">
            </div>
            <div class="p-3 text-end">
                <button type="submit" class="btn btn-success">save</button>
            </div>
        </form>
        <hr>
        <% Object.values(data).forEach(function(item){ %>
            <div class="border border-black p-3">
                <a href="/<%= item.id %>">id :<%= item.id %></a>
                <p>ชื่อ :<%= item.name %></p>
                <p>อายุ :<%= item.age %></p>
                <p>เงินเดือน :<%= item.salary %></p>
                <div class="text-end">
                    <a class="btn btn-danger" href="/edit/<%= item.id %>">แก้ไข</a>
                    <a class="btn btn-danger" onclick="return confirm('คุณต้องการลบข้อมูลหรือไม่?')" href="/delete/<%= item.id %>">ลบ</a>
                </div>
            </div>
        <% }) %>
    </div>
</body>
</html>